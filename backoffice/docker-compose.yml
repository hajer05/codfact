services:
  backoffice:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: backoffice-app
    ports:
      - "4200:4200"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
    networks:
      - backoffice-net

networks:
  backoffice-net:
    driver: bridge


